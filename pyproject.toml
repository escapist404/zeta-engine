[project]
name = "zeta-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "argparse>=1.4.0",
    "beautifulsoup4>=4.15.0",
    "jieba>=0.42.1",
    "requests>=2.34.2",
    "url-normalize>=3.0.0",
]

[project.scripts]
zeta-engine = "zeta_engine.cli:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
